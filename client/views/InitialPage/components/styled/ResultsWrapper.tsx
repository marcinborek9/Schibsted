import styled, {StyledComponent} from 'styled-components';

export const ResultsWrapper: StyledComponent<"div", any> = styled.div`
  max-width: 90%;
  display: flex;
  flex-wrap: wrap;
  margin: 0 auto;
  align-items: center;
  justify-content: center;
`
